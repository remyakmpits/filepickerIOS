import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ios_file_picker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
